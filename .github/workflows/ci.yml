name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ['3.10', '3.11', '3.12']

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          pip install pytest-cov

      - name: Download test fixtures
        run: |
          mkdir -p tests/fixtures

          # Academic paper (arxiv always works)
          curl -sL -o tests/fixtures/attention.pdf "https://arxiv.org/pdf/1706.03762"

          # Generate simple test PDFs with pymupdf
          python3 tests/generate_fixtures.py 2>/dev/null || echo "Fixture generation failed, some tests will skip"

          ls -la tests/fixtures/
        shell: bash

      - name: Run linter
        run: |
          pip install ruff
          ruff check pdf_pilot/ tests/

      - name: Run tests with coverage
        run: |
          pytest tests/test_detectors.py tests/test_router.py tests/test_engines.py::TestPyMuPDFEngine tests/test_quality.py \
            -v --tb=short \
            --cov=pdf_pilot --cov-report=xml --cov-report=term-missing
        env:
          PYTHONPATH: .

      - name: Upload coverage
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.12'
        uses: codecov/codecov-action@v4
        with:
          file: coverage.xml
          fail_ci_if_error: false
          verbose: true
